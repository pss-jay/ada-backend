# Ada Backend - Dependencies
# Core (already available in most Python 3.10+ environments)
pdfplumber>=0.10.0
python-docx>=1.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# MongoDB / Cosmos DB (production storage)
pymongo>=4.5.0
certifi>=2023.0.0
